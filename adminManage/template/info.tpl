info(
	title: "{{.Title}}"
	desc: "{{.Desc}}"
	version: "{{.Version}}"
	author: "{{.Author}}"
	email: "{{.Email}}"

	host: {{.Host}}
	port: {{.Port}}
	commonPkgPath: {{.CommonPkgPath}}
	dataSource: {{.DataSource}}
	cacheHost: {{.CacheHost}}
)
