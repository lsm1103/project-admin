info(
	title: "{{.Title}}"
	desc: "{{.Desc}}"
	version: "{{.Version}}"
	author: "{{.Author}}"
	email: "{{.Email}}"

	projectName: {{.ProjectName}}
	host: {{.Host}}
	port: {{.Port}}
	dataSource: {{.DataSource}}
	cacheHost: {{.CacheHost}}
)
